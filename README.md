# 📝 Lista de Tarefas

Aplicação simples de lista de tarefas (to-do list) feita com HTML, CSS e JavaScript puros, com persistência dos dados no navegador via `localStorage`.

## 📋 Sobre o projeto

Uma interface leve para adicionar e remover tarefas do dia a dia. As tarefas ficam salvas no `localStorage`, então continuam disponíveis mesmo depois de fechar ou atualizar a página.

## ✨ Funcionalidades

- Adicionar novas tarefas pelo botão **Adicionar** ou pressionando **Enter**
- Remover tarefas individualmente
- Persistência automática das tarefas no navegador (`localStorage`)
- Mensagem indicando quando a lista está vazia
- Layout responsivo, adaptado também para telas pequenas

## 🛠️ Tecnologias utilizadas

- **HTML5** — estrutura da página
- **CSS3** — estilização, responsividade e pequenas animações
- **JavaScript** — lógica de adicionar/remover tarefas e persistência de dados

## 📁 Estrutura do projeto

```
lista-tarefas/
├── index.html    # Página principal
├── script.js     # Lógica da aplicação
└── README.md
```

## 🚀 Como executar o projeto

1. Clone este repositório:
   ```bash
   git clone <url-do-repositorio>
   ```
2. Acesse a pasta do projeto:
   ```bash
   cd lista-tarefas
   ```
3. Abra o arquivo `index.html` no seu navegador de preferência.

Não é necessário nenhuma instalação de dependências — o projeto utiliza apenas HTML, CSS e JavaScript nativos.

## 👨‍💻 Autor

Desenvolvido como projeto de estudo de manipulação do DOM e `localStorage` em JavaScript.